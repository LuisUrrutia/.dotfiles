[pre-start]
copy-local-git-config = "wt step copy-ignored --require-include"

[pre-merge]
verify = "./dotfiles verify"

[commit.generation]
template-append = """
- Use the repository's conventional commit style.
- Keep the subject focused on one atomic behavior change.
"""
