# label: cloud and infrastructure tools
# question: Do you work with AWS, Terraform, or cloud infrastructure?
# summary: gcloud, AWS CLI, kubectl, Helm, Terraform, Cosign
# aliases: infra, infrastructure, aws

cask "gcloud-cli"              # Google Cloud CLI
brew "awscli"                  # AWS CLI
brew "aws-sam-cli"             # AWS SAM CLI
brew "kubernetes-cli"          # Kubernetes CLI (kubectl)
brew "helm"                    # Kubernetes package manager
brew "cosign"                  # Container signing and verification
brew "hashicorp/tap/terraform", trusted: true # Infrastructure as code
